# 部署说明（腾讯云 CloudBase 云托管）

## 一、准备

1. 腾讯云账号 + 实名认证（建议做学生认证）
2. 代码托管账号：Gitee（推荐）或 GitHub

## 二、创建云开发环境

1. 腾讯云控制台搜索「云开发 CloudBase」
2. 新建环境 → 按量付费 → 选择就近地域（上海/广州）
3. 记录环境 ID

## 三、推送代码

项目已经初始化 git 并完成首次提交，Dockerfile 与 .dockerignore 已就绪。

在 Gitee/GitHub 新建空仓库后执行：

```bash
git remote add origin <仓库地址>
git push -u origin main
```

注意：`.env.local` 已被忽略，不会上传；真实 Key 只配置在云托管的环境变量里。

## 四、创建云托管服务

在云开发控制台进入「云托管」→ 新建服务：

- 部署方式：代码仓库（关联 Gitee/GitHub）
- 分支：main
- 构建方式：Dockerfile
- 容器端口：3000
- 规格：最小规格（0.25C0.5G 或 0.5C1G），最小实例数 0（省钱）
- 环境变量：
  - `DEEPSEEK_API_KEY` = 你的 Key
  - `DEEPSEEK_BASE_URL` = `https://api.deepseek.com`
- 点击部署

## 五、验证

部署完成后使用默认域名访问，依次验证：

1. 登录页品牌显示「搭友」
2. 演示账号 123123 / 123123 可登录
3. AI 匹配返回结果并显示耗时与成本
4. 发布需求成功
5. 校园圈可发帖、点赞、评论
6. 管理端 admin / admin123 可进入

## 六、注意事项

- 默认域名由腾讯云提供，无需自行备案
- 演示版数据存储在浏览器本地，每个访客的数据互相独立
- AI 调用会消耗 DeepSeek 额度，建议账户只放少量余额
- 如需自定义域名，需要完成 ICP 备案
